2026年3月6日21:19:14 需求：

现在要实现对data_items表数据内容的再次过滤。

我在data_items中增加了三个字段： 表示是否推荐，推荐理由，推荐处理时间
```
  "recommend" bool,
  "recommend_reason" text COLLATE "pg_catalog"."default",
  "recommend_at" timestamp(6),
```

1 - 在系统设置中增加一个很高的多行文本框，用来输入个人特点的描述，作为推荐判断的基础。
2 - 推荐系统作为一个插件，根据设定频率进行调用，根据系统设置中的个人特点描述，由LLM来确认该内容是适合用户的想法。符合想法时，recommend 为 true， 并赋予reason，更新recommend_at。如果不符合，则只更新recommend_at。  注意时间的时区是Shanghai。
3 - 为recommend逻辑生成单独的prompt，可以参考summary的prompt。
4 - 因为recommend要调用免费的模型，要有单独的模型配置，每次处理前100个未处理（recommend_at为NULL）。
5 - 我看到数据库里有plugins表、plugin_schedules表，这其中的数据应该如何初始化。